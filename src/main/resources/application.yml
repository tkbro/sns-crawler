spring:
    data:
        mongodb:
            host: localhost
            database: projectdb
            port: 27017
            username: beom
            password: ""
            auto-index-creation: true

discord:
    endpoint:
    channel: "일반"

selenium:
    chrome-driver-path: D:/programming/WebDriver/bin/chromedriver.exe
